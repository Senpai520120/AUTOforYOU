# Тест-кейсы E2E

Разбор всех автотестов Playwright: что проверяется, к какому типу относится, и чего не покрыто.

Backend-тесты (Django, 394 шт.) сюда не входят — по ним отдельная задача, Issue #7.

---

## Как запускать

Тестам нужен поднятый стек — они ходят по настоящему сайту, а не по моку.

```powershell
cd D:\pyCharm\AUTOforYOU
docker compose up -d
```

Ждём, пока `backend` и `db` покажут `(healthy)`:

```powershell
docker compose ps
```

Дальше **обязательно из `frontend/`**:

```powershell
cd frontend
npx playwright test
```

Запуск из корня репозитория не сработает: там нет ни `node_modules`, ни конфига, и `npx` подтянет из реестра другую версию Playwright. Раннер и библиотека окажутся разными экземплярами, и прогон упадёт с `Playwright Test did not expect test.describe() to be called here`. Признак ошибки — пути вида `frontend\e2e\catalog.spec.ts` в выводе вместо `catalog.spec.ts:16:7`.

| Команда | Что делает |
|---|---|
| `npx playwright test` | Полный прогон |
| `npm run test:e2e:ui` | UI-режим: дерево тестов, watch, trace на лету |
| `npm run test:e2e:headed` | С видимым браузером |
| `npm run test:e2e:debug` | Инспектор с пошаговым выполнением и Pick locator |
| `npm run test:e2e:report` | Открыть HTML-отчёт последнего прогона |
| `npm run test:e2e:codegen` | Запись действий в код |
| `npx playwright test -g "текст"` | Только тесты, чьё имя содержит текст |
| `npx playwright show-trace <путь к trace.zip>` | Trace Viewer: таймлайн, DOM на каждом шаге, сеть, консоль |

Видео, trace и скриншот пишутся **только для упавших** тестов — `retain-on-failure` в конфиге. У зелёных артефактов нет.

---

## Сводка

**33 теста в 6 файлах**, прогон около 18 секунд.

| Тип | Кол-во |
|---|---|
| Happy path | 20 |
| Negative path | 12 |
| Проверка отсутствия элемента | 1 |

| Файл | Тестов | О чём |
|---|---|---|
| `home.spec.ts` | 5 | Главная страница |
| `test-1.spec.ts` | 5 | Шапка сайта |
| `catalog.spec.ts` | 5 | Каталог импорта и калькулятор |
| `register.spec.ts` | 5 | Регистрация и согласие с условиями |
| `negative.spec.ts` | 9 | Валидация форм |
| `ssr.spec.ts` | 4 | Серверный рендер и sitemap |

---

## Happy path

### Главная страница — `home.spec.ts`

| Тест | Что проверяет |
|---|---|
| hero веде в калькулятор і в каталог | Заголовок виден, кнопка ведёт на `/calculator` |
| блок «Два напрямки» розводить імпорт і локальний каталог | Две ссылки указывают на `/listings` и `/ua` |
| «Як це працює» містить чотири етапи по порядку | Ровно 4 шага, каждый на своём месте |
| блок вартості перелічує статті і веде в калькулятор | Названы мито 10%, ПДВ 20%, пенсійний збір; кнопка открывает калькулятор |
| футер описує обидва напрямки | Есть «Оголошення по Україні», нет «маркетплейс авто з США» |

### Шапка — `test-1.spec.ts`

| Тест | Что проверяет |
|---|---|
| логотип веде на головну | С `/calculator` клик по лого возвращает на `/` |
| навігація веде на три основні розділи | `href` у «Пригін/аукціон», «Каталог Україна», «Калькулятор» |
| гостю показані вхід і реєстрація | Ссылки на `/login` и `/register` |
| перехід у каталог України через шапку | Клик приводит на `/ua`, заголовок на месте |

### Каталог и калькулятор — `catalog.spec.ts`

| Тест | Что проверяет |
|---|---|
| сторінка каталогу відповідає 200 і рендерить заголовок | Статус ответа и `h1` |
| форма містить підписані поля і демо-банер | Поля находятся **по подписи** (`getByLabel`), баннер виден |
| розрахунок повертає повну деталізацію вартості | Заполнение формы → все статьи разбивки → итог не прочерк |

### Регистрация — `register.spec.ts`

| Тест | Что проверяет |
|---|---|
| форма містить поля і чекбокс згоди | Поле email и чекбокс, чекбокс снят по умолчанию |
| посилання «Умовами використання» відкриває сторінку умов | Реальный переход в новую вкладку, URL и заголовок |
| посилання «Політикою конфіденційності» відкриває сторінку політики | То же для `/privacy` |
| футер містить посилання на юридичні документи | Четыре ссылки внутри `contentinfo` |

### Серверный рендер — `ssr.spec.ts`

Все четыре гоняются с **выключенным JavaScript** — это то, что видит поисковый робот без выполнения скриптов.

| Тест | Что проверяет |
|---|---|
| каталог України віддає вміст без JavaScript | Заголовок, содержимое каталога, отсутствие скелетона |
| каталог імпорту віддає вміст без JavaScript | То же для `/listings` |
| головна віддає блоки без JavaScript | Hero и обе секции в HTML |
| sitemap містить каталог України | `/ua`, `/listings`, `/refund` в `sitemap.xml` |

---

## Negative path

Этой части ТЗ уделяет отдельное внимание — и не зря: до недавнего времени негативных кейсов в проекте почти не было, suite проверял только счастливый путь.

### Регистрация — `negative.spec.ts`

| Тест | Некорректный ввод | Ожидаемая реакция | Чем подтверждается |
|---|---|---|---|
| невалідний email блокується браузером | `не-email` | Форма не отправляется | `validity.typeMismatch === true` |
| паролі, що не збігаються | Разные пароли | Ошибка от сервера | Текст «Паролі не збігаються» виден |
| повторна реєстрація на той самий email | Второй раз тот же email | Отказ | Остались на `/register`, показана ошибка |
| надто простий пароль | `12345678` | Отказ валидатора Django | Остались на `/register`, показана ошибка |

### Калькулятор — `negative.spec.ts`

| Тест | Некорректный ввод | Чем подтверждается |
|---|---|---|
| відʼємна ціна | `-5000` | `validity.rangeUnderflow === true` |
| нечислова ціна | Буквы с клавиатуры | Поле осталось пустым, `validity.valueMissing === true` |
| обʼєм двигуна понад ліміт | `99999` при `max=10000` | `validity.rangeOverflow === true` |
| рік випуску раніше 1990 | `1200` при `min=1990` | `validity.rangeUnderflow === true` |
| порожня форма | Все поля пустые | Результат не появился |

### Прочие негативные

| Тест | Файл | Что проверяет |
|---|---|---|
| без згоди форма не відправляється | `register.spec.ts` | Чекбокс `required` блокирует отправку |
| невідомий маршрут повертає 404 | `catalog.spec.ts` | Статус 404 и страница «Сторінку не знайдено» |
| гостю не показані розділи кабінету | `test-1.spec.ts` | Нет уведомлений, сообщений и кнопки «Вийти» |

### Проверка отсутствия элемента

| Тест | Файл | Что проверяет |
|---|---|---|
| демо-банер на каталозі не показується | `catalog.spec.ts` | `DemoBanner` рендерится только на `/calculator` |

---

## Как отличить настоящий тест от фальшиво-зелёного

Правило простое: **зелёный прогон ничего не доказывает**. Доказывает только то, что тест краснеет, когда проверяемое поведение ломается.

В этом проекте фальшиво-зелёные тесты уже были — три из девяти. Полезно помнить, как они выглядели.

**1. Локатор объявлен и не проверен**

```ts
test('demo banner is visible on catalog page', async ({ page }) => {
  await page.goto('/listings');
  const banner = page.locator('[role="alert"]');   // объявлен
  await expect(page.locator('main')).toBeVisible(); // а проверяется main
});
```

Тест зелёный всегда. Причём `DemoBanner` на `/listings` не рендерится вообще — проверять там было нечего.

**2. Действие обёрнуто в условие**

```ts
const submitBtn = page.locator('button[type="submit"]');
if (await submitBtn.count() > 0) {
  await submitBtn.click();
  await page.waitForTimeout(3000);
}
```

Исчезнет кнопка — тест останется зелёным. Плюс `waitForTimeout` вместо ожидания результата: три секунды на пустом месте.

**3. Бессмысленный ассерт**

```ts
await expect(page.locator('text=500')).not.toBeVisible();
```

Страница ошибки Django и Next так не выглядит. Проверка не сработает никогда.

### Приём: проверка мутацией

Перед тем как считать тест готовым — сломать проверяемое поведение и убедиться, что тест покраснел.

Так проверялись тесты калькулятора: из формы удалили **один** `htmlFor`, пересобрали контейнер, прогнали. Покраснели ровно два теста из пяти — те, что зависят от этого поля. Три остальных остались зелёными. После этого разметку восстановили.

Так же проверялись тесты SSR: каталоги откатили к клиентской версии — покраснели ровно два теста серверного рендера, домашняя и sitemap не затронуты.

### Приём: подтверждать причину, а не только следствие

Негативный тест, который проверяет только отсутствие результата, останется зелёным и тогда, когда форма сломалась целиком и не отправляется вообще.

```ts
await expect(page.getByRole('heading', { name: RESULT })).toHaveCount(0);   // следствие
expect(await price.evaluate((el: HTMLInputElement) => el.validity.rangeUnderflow)).toBe(true);  // причина
```

---

## Чего не покрыто и почему

| Что | Почему |
|---|---|
| Блок «Свіжі авто» на главной | Рендерится на сервере и только при наличии лотов. SSR-запрос нельзя перехватить через `page.route`, а проверка «якщо блок є, то...» была бы зелёной всегда. Подтверждается вручную и через `curl` по серверному HTML |
| Личный кабинет, сообщения, сделки, избранное | Нужна авторизация и подготовленные данные. Backend-тесты это покрывают, E2E — нет |
| Оплата через LiqPay | Нужны реальные ключи и публичный HTTPS-адрес для колбэка |
| B2B-доска | Нужен верифицированный дилер в БД |
| Подача объявления целиком | Нужна модерация: объявление уходит в `pending` и не появляется в каталоге |
| Мобильная вёрстка | Конфиг содержит только проект `chromium` с Desktop Chrome |
| Другие браузеры | Firefox и WebKit не подключены |

---

## Что стоит добавить дальше

1. **Авторизованные сценарии.** Логин через API в `globalSetup`, сохранение `storageState` — тогда откроются кабинет, избранное, сообщения.
2. **Мобильный проект** в `playwright.config.ts` — сайт адаптивный, но это не проверяется.
3. **Полный цикл подачи объявления**: подача → модерация через API администратором → появление в каталоге.
4. **Визуальные снапшоты** ключевых страниц — ловят вёрстку, которую текстовые ассерты не видят.
